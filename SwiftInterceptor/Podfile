# Uncomment the next line to define a global platform for your project
platform :ios, '8.0'
use_frameworks!

target 'SwiftInterceptor' do

  target 'SwiftInterceptorTests' do
    inherit! :search_paths
  end

end
